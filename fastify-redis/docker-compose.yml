version: '3'

services:

  redisdb:
    build:
      context: ../redis
    volumes:
      - /opt/microservices/redis/:/data/
#    ports:
#      - 5984:5984

  fastify:
    build: fastify
    ports:
      - 10003:3000
    depends_on:
      - redisdb
    restart: always